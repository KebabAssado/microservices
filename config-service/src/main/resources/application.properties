spring.application.name=config-service
server.port=8888

spring.cloud.config.server.git.uri=https://github.com/luciano-ferretto/microservices-java-2026.git
spring.cloud.config.server.git.search-paths=configs/{application}
spring.cloud.config.server.git.default-label=main

# Self Registration Pattern
eureka.client.service-url.default-zone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}
eureka.client.healthcheck.enabled=true

# Health Check API Pattern
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.info.env.enabled=true
management.endpoint.health.probes.enabled=true
management.health.defaults.enabled=true

# Info endpoint
info.app.name=${spring.application.name}
info.app.description=Config Service - Spring Cloud Config Server
info.app.autor=Kevin

# Metadata Eureka
eureka.instance.metadata-map.github.repo=KebabAssado/microservices